package com.richest.manager.service;

import com.richest.manager.domain.Campaign;
import com.richest.manager.domain.Process;

public interface IProcessService {

	Process addProcess(Process process, Campaign campaign);

	Process updateProcess(Process process, Campaign campaign);

	Integer addRgProcesses(Integer campaignId);

}
